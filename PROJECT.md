# 임태형 포트폴리오 사이트 — 프로젝트 문서

> 이 문서는 **이 프로젝트를 처음 보는 사람**이 코드를 열지 않고도 전체를 파악할 수 있도록
> 작성되었습니다. 무엇을 만들었는지, 왜 그렇게 만들었는지, 어디를 어떻게 고치면 되는지를
> 순서대로 설명합니다.
>
> - 간단한 소개만 필요하다면 → [`README.md`](./README.md)
> - 지금 무엇을 해야 하는지만 알고 싶다면 → [11. 남은 작업](#11-남은-작업)

**최초 작성일**: 2026-08-25

---

## 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [요구사항](#2-요구사항)
3. [기술 선택과 근거](#3-기술-선택과-근거)
4. [파일 구조](#4-파일-구조)
5. [디자인 시스템](#5-디자인-시스템)
6. [페이지 구조](#6-페이지-구조)
7. [기능 구현 상세](#7-기능-구현-상세)
8. [접근성](#8-접근성)
9. [실행과 검증](#9-실행과-검증)
10. [자주 하는 수정 방법](#10-자주-하는-수정-방법)
11. [남은 작업](#11-남은-작업)
12. [배포 절차](#12-배포-절차)
13. [면접에서 설명할 수 있는 포인트](#13-면접에서-설명할-수-있는-포인트)
14. [알려진 제약](#14-알려진-제약)

---

## 1. 프로젝트 개요

### 무엇인가

프론트엔드 개발자 **임태형**의 개인 포트폴리오 웹사이트입니다.
기업에 지원할 때 이력서와 함께 제출할 링크로 사용하는 것이 목적입니다.

### 누구를 위한 것인가

**주 독자는 채용 담당자와 면접관**입니다. 일반 방문자가 아닙니다.
이 전제가 설계 전반을 결정했습니다.

- 짧은 시간(수십 초) 안에 "이 사람이 무엇을 할 수 있는가"가 전달되어야 한다
- 링크를 눌렀을 때 즉시 떠야 한다 (로딩이 느리면 그 자체가 감점)
- 개발자가 개발자 도구를 열어볼 수 있으므로, **코드 자체도 결과물**이다

### 현재 상태

**구조와 기능은 완성**되었고, **본문 내용은 자리표시자(`[TODO]`) 상태**입니다.
디자인·인터랙션·접근성 대응은 모두 동작하며, 실제 텍스트와 이미지만 채우면 바로 배포할 수 있습니다.

| 항목 | 상태 |
|---|---|
| 마크업 / 스타일 / 스크립트 | 완료 |
| 다크 모드, 한·영 전환, 스크롤 인터랙션 | 완료 |
| 반응형, 접근성 대응 | 완료 |
| 정합성 검증 | 통과 |
| 본문 텍스트 (소개·프로젝트·학력) | 자리표시자 |
| 이미지 (프로필·스크린샷) | 자리표시자 SVG |
| 배포 | 미진행 |

### 규모

총 약 2,300줄, 외부 런타임 의존성 0개.

| 파일 | 줄 수 | 역할 |
|---|---:|---|
| `index.html` | 491 | 전체 마크업 |
| `css/style.css` | 888 | 레이아웃 및 컴포넌트 |
| `css/tokens.css` | 144 | 디자인 토큰 + 다크 테마 |
| `css/reset.css` | 116 | 최소 리셋 |
| `js/main.js` | 287 | 헤더·메뉴·스크롤·복사 |
| `js/i18n.js` | 200 | 한/영 전환 |
| `js/theme.js` | 86 | 라이트/다크 전환 |

---

## 2. 요구사항

### 인물 정보

| 항목 | 내용 |
|---|---|
| 이름 | 임태형 |
| 희망 직무 | 프론트엔드 개발자 (신입, 실무 경험 없음) |
| 이메일 | dlaxogud1226@naver.com |
| GitHub | [THLIMM](https://github.com/THLIMM) |

### 디자인 방향

원문 요구사항은 다음과 같았습니다.

> 모던하고 깔끔하게. 웹페이지를 보았을 때 이런저런 기교 없이 한눈에 깔끔하게 나에 대해
> 보여줄 수 있고, 또한 프론트엔드인 만큼 나름의 포인트들이 들어가 있는 사이트

이를 두 개의 원칙으로 정리했습니다.

1. **기교 없이** — 타이핑 효과, 커서 추적, 패럴랙스 같은 눈에 띄는 연출은 넣지 않는다.
2. **포인트는 있게** — 대신 *잘 만든 사이트에서만 느껴지는* 디테일을 넣는다.
   깜빡임 없는 테마 전환, 자연스러운 등장, 현재 위치 표시, 키보드 대응 등.

### 참고 사이트

[Simplefolio](https://the-simplefolio.netlify.app/) — Hero → About → Projects → Contact의
단순한 원페이지 구조를 참고했습니다. 다만 그대로 따르지 않고 두 가지를 바꿨습니다.

- **Skills 섹션을 추가** — 신입 채용에서 기술 스택은 반드시 확인하는 항목이기 때문
- **Education 섹션을 추가** — 실무 경력이 없는 신입에게는 교육 이력이 경력을 대신하기 때문

---

## 3. 기술 선택과 근거

### 결정 요약

| 항목 | 결정 | 핵심 근거 |
|---|---|---|
| 빌드 방식 | 순수 HTML/CSS/JS (빌드 도구 없음) | 정적 원페이지에 프레임워크는 과함. CSS 기본기를 드러낼 수 있음 |
| 스타일링 | 직접 작성한 CSS + 커스텀 프로퍼티 | Tailwind는 이미 경험이 있어 반복. 직접 CSS는 새 증거가 됨 |
| 언어 | 한국어 기본 + 영어 토글 | 국내 지원이 주목적, 토글 자체가 인터랙션 포인트 |
| 테마 | 라이트/다크 토글 | CSS 변수 기반이라 프레임워크 없이도 깔끔하게 구현됨 |
| 프로젝트 레이아웃 | 2개, 가로형 지그재그 | 개수가 적으므로 카드 그리드보다 각각을 깊게 보여주는 편이 유리 |
| 스킬 표기 | 3단계 분리 | 미검증 스택을 섞어 적으면 면접에서 역효과 |
| 배포 | GitHub → Vercel | 본인이 직접 배포를 경험해보는 것이 목표 중 하나 |

### 왜 프레임워크를 쓰지 않았는가

지원자의 주력 스택은 React / Next.js입니다. 그럼에도 이 사이트는 순수 HTML/CSS/JS로 만들었고,
그 이유는 다음 네 가지입니다.

**1) 이 사이트에는 React가 할 일이 없다**

원페이지 정적 사이트라 서버 데이터 페칭이 없고, 상태는 테마·언어·메뉴 열림 세 가지뿐이며,
반복 렌더링할 목록은 프로젝트 2개가 전부입니다. React를 쓰면 "React로 만들었다"는 사실만
남고 **React 실력은 드러나지 않습니다.**

**2) 대신 CSS 기본기가 드러난다**

개발자 도구를 열었을 때 보이는 것이 다릅니다. Grid/Flex 레이아웃, `clamp()` 기반 타입 스케일,
`prefers-color-scheme`, 시맨틱 마크업 — 이것들을 직접 짤 줄 아는지는 신입 평가에서 변별력이
있습니다. Tailwind만 써온 지원자가 많기 때문입니다.

**3) 이미 Tailwind 경험이 있다**

Tailwind로 만들면 *이미 가진 스킬을 반복*하는 것이고, 직접 CSS로 만들면 *없던 증거를 추가*하는
것입니다. 같은 시간을 쓴다면 후자가 남는 것이 많습니다.

**4) 첫인상은 로딩 속도가 만든다**

외부 스크립트가 하나도 없어 JS 번들이 0KB입니다. 링크를 누른 채용 담당자가 기다릴 일이 없습니다.

### 예상되는 반문과 답변

> "React 지원자인데 왜 정적 HTML인가?"

면접에서 나올 수 있는 질문입니다. 다음과 같이 답하면 오히려 유리합니다.

> "정적 원페이지에 프레임워크를 도입하는 것은 오버엔지니어링이라 판단했습니다.
> 기술 선택은 요구사항에 맞춰야 한다고 생각해서 의도적으로 배제했고,
> React 역량은 프로젝트 섹션에서 보여드리려 했습니다."

**기술 선택의 근거를 설명할 줄 아는 것** 자체가 평가 대상입니다.

### 채택하지 않은 대안

| 대안 | 배제 이유 |
|---|---|
| Vite + Tailwind CSS | 이미 경험이 있어 스택 추가 효과가 없고, 결과물 HTML의 가독성이 떨어짐 |
| Next.js + TypeScript | 정적 페이지에 과한 구성이고 제작 시간이 더 걸림 |
| CSS 프레임워크 (Bootstrap 등) | 디자인이 템플릿처럼 보여 차별화가 어려움 |
| 스킬 숙련도 퍼센트 바 | 근거 없는 수치는 면접에서 반드시 반박당함 |

---

## 4. 파일 구조

```
my-profile-site/
├─ index.html                  전체 마크업 (한국어 본문이 그대로 들어 있음)
│
├─ css/
│  ├─ reset.css                브라우저 기본 스타일 중 방해되는 것만 제거
│  ├─ tokens.css               디자인 토큰 (색·타이포·간격) + 다크 테마 정의
│  └─ style.css                레이아웃 및 컴포넌트 (14개 섹션으로 구획)
│
├─ js/
│  ├─ i18n.js                  한/영 전환 — 영어 사전과 전환 로직
│  ├─ theme.js                 라이트/다크 전환 — 토글 버튼 동작
│  └─ main.js                  헤더·모바일 메뉴·스크롤·이메일 복사
│
├─ assets/
│  └─ img/
│     ├─ favicon.svg           파비콘 (파란 사각형 + "TH")
│     ├─ profile-placeholder.svg    프로필 사진 자리 (교체 대상)
│     └─ project-placeholder.svg    프로젝트 스크린샷 자리 (교체 대상)
│
├─ README.md                   짧은 소개와 실행 방법
├─ PROJECT.md                  이 문서
└─ .gitignore
```

### 로드 순서와 그 이유

`index.html`의 `<head>`와 `<body>` 끝에서 다음 순서로 로드됩니다.

```
<head>
  1. Pretendard 웹폰트 (CDN)
  2. reset.css → tokens.css → style.css
  3. 인라인 스크립트            ← 첫 페인트 전에 테마 확정 (7.1 참고)
<body 끝>
  4. i18n.js → theme.js → main.js
```

- **CSS 순서가 중요합니다.** `reset` → `tokens` → `style` 순서여야 뒤 파일이 앞 파일을
  덮어쓸 수 있습니다.
- **인라인 스크립트는 `<head>`에 있어야 합니다.** `<body>` 끝으로 옮기면 테마가 깜빡입니다.
- **`i18n.js`가 `main.js`보다 먼저** 로드됩니다. `main.js`의 이메일 복사 기능이
  `window.i18n.t()`를 사용하기 때문입니다.

### CSS 파일 내부 구획

`style.css`는 주석으로 14개 구획을 나눠 두었습니다. 찾을 때 참고하세요.

| # | 구획 | 시작 줄 |
|---:|---|---:|
| 1 | 기본 (body, 링크, 포커스, skip-link) | 6 |
| 2 | 레이아웃 (container, section) | 58 |
| 3 | 버튼 | 109 |
| 4 | 태그 | 158 |
| 5 | 헤더 / 내비게이션 | 197 |
| 6 | Hero | 365 |
| 7 | About | 451 |
| 8 | Skills | 487 |
| 9 | Projects | 544 |
| 10 | Education 타임라인 | 632 |
| 11 | Contact | 690 |
| 12 | Footer | 768 |
| 13 | 등장 애니메이션 | 794 |
| 14 | 반응형 (미디어쿼리) | 811 |

> 미디어쿼리를 **파일 맨 끝에 모아 두었습니다.** 각 컴포넌트 옆에 흩어 두는 방식도 있지만,
> 이 규모에서는 "넓은 화면에서 무엇이 달라지는가"를 한 곳에서 보는 편이 파악하기 쉽습니다.

---

## 5. 디자인 시스템

### 핵심 아이디어

색·간격·타이포를 전부 **CSS 커스텀 프로퍼티(변수)** 로 `tokens.css`에 모았습니다.
하드코딩된 색상값이 `style.css`에 하나도 없습니다.

그래서 **사이트 전체 톤은 변수 한 줄로 바뀝니다.**

```css
--accent: #2563eb;   /* 이 값만 바꾸면 버튼·링크·강조·타임라인 점이 모두 따라 바뀜 */
```

### 색상 토큰

| 토큰 | 라이트 | 다크 | 용도 |
|---|---|---|---|
| `--bg` | `#ffffff` | `#0d0f14` | 페이지 배경 |
| `--bg-subtle` | `#f7f8fa` | `#14171e` | 번갈아 나오는 섹션 배경 |
| `--surface-hover` | `#f2f4f7` | `#1e222c` | 태그 배경, 호버 상태 |
| `--text` | `#16181d` | `#e8eaed` | 본문 |
| `--text-muted` | `#5b6270` | `#9aa3b2` | 설명문 |
| `--text-faint` | `#8b93a1` | `#6b7280` | 날짜, 푸터 |
| `--border` | `#e5e7eb` | `#262b36` | 기본 경계선 |
| `--border-strong` | `#d1d5db` | `#363c4a` | 강조 경계선, 버튼 테두리 |
| `--accent` | `#2563eb` | `#60a5fa` | 강조색 |
| `--accent-soft` | `#eff6ff` | `#16233a` | 강조색 배경 |

> 다크 모드의 `--accent`가 라이트보다 밝은 것은 의도된 것입니다.
> 어두운 배경 위에서 진한 파랑은 대비가 부족해 읽히지 않습니다.

### 타이포그래피

**서체는 Pretendard 하나만 사용합니다.** 한글과 영문을 모두 커버하기 때문에
언어를 전환해도 화면의 인상이 흔들리지 않습니다. 코드성 텍스트(날짜, 섹션 번호, 기술 태그)에만
`--font-mono`를 씁니다.

크기는 `clamp()`로 정의해 뷰포트에 따라 **미디어쿼리 없이** 부드럽게 변합니다.

| 토큰 | 값 | 용도 |
|---|---|---|
| `--fs-display` | `clamp(2.5rem, 6vw, 4.5rem)` | Hero의 이름 |
| `--fs-h2` | `clamp(1.75rem, 3.5vw, 2.5rem)` | 섹션 제목 |
| `--fs-h3` | `clamp(1.25rem, 2vw, 1.5rem)` | 프로젝트·타임라인 제목 |
| `--fs-lg` / `--fs-base` / `--fs-sm` / `--fs-xs` | `1.125` / `1` / `0.875` / `0.8125rem` | 본문 계열 |

### 간격

4px 배수 스케일(`--sp-1` = 4px ~ `--sp-24` = 96px)을 씁니다.
섹션 상하 여백은 `--section-py: clamp(4.5rem, 10vw, 7.5rem)` 으로, 모바일에서 자동으로 줄어듭니다.

### 브레이크포인트

모바일 퍼스트로 작성하고 `min-width`로 넓은 화면을 덧붙입니다.

| 폭 | 달라지는 것 |
|---|---|
| 기본 (~639px) | 모든 것이 1열. 내비게이션은 햄버거 + 전체 화면 오버레이 |
| `640px~` | About이 2열(사진 + 글)로 나뉨 |
| `768px~` | 내비게이션이 인라인 메뉴로 복귀, 헤더 높이 64→72px, Skills가 2열 |
| `900px~` | 프로젝트가 2열이 되고 지그재그 배치 시작 |

### 클래스 이름 규칙 (BEM)

```
.block            컴포넌트          예: .project
.block__element   내부 요소         예: .project__title
.block--modifier  변형              예: .project--reverse
.is-state         JS가 붙이는 상태   예: .is-open, .is-active, .is-visible
```

**상태 클래스는 `is-` 접두사로 통일**했습니다. 이 접두사가 붙은 클래스는
JS가 제어한다는 뜻이므로, CSS만 보고도 어떤 것이 동적인지 구분됩니다.

---

## 6. 페이지 구조

원페이지 구성이며, 위에서 아래로 다음 순서입니다.

```
Header (고정)
  └ 로고 · 메뉴 5개 · 언어 토글 · 테마 토글 · (모바일) 햄버거
Hero              화면 전체 높이
About        01
Skills       02   ← 배경색 다름 (--bg-subtle)
Projects     03
Education    04   ← 배경색 다름
Contact      05
Footer
```

> 섹션 배경색을 번갈아 준 것은 스크롤할 때 구분감을 만들기 위한 것입니다.
> 선을 긋는 것보다 조용하면서 효과가 확실합니다.

### 섹션별 상세

#### Hero

```
Frontend Developer          ← 모노스페이스, 대문자, 강조색
임태형                      ← 가장 큰 글씨
[한 줄 소개]
[프로젝트 보기] [연락하기]
                ↓          ← 아래로 이어진다는 힌트 (얇은 선이 천천히 내려감)
```

- 화면 높이 전체(`100svh`)를 차지합니다. `vh`가 아니라 **`svh`** 를 쓴 이유는
  모바일 브라우저의 주소창이 접혔다 펴질 때 높이가 튀는 것을 막기 위해서입니다.
- 4개 요소가 80ms 간격으로 순차 등장합니다 (`--reveal-delay` 인라인 지정).

#### About

프로필 사진 + 소개 문단 3개. 640px 이상에서 2열입니다.
문단 구성은 다음을 의도했습니다.

1. 어떤 개발자인지
2. 무엇을 해왔는지
3. 어디로 가고 싶은지

이력서 PDF 다운로드 버튼이 `index.html:195`에 **주석 처리되어** 있습니다.
`assets/resume.pdf`를 넣은 뒤 주석을 해제하면 됩니다.

#### Skills — 3단계 분리

이 사이트에서 **가장 의도가 많이 들어간 섹션**입니다.

| 단계 | 의미 | 현재 항목 |
|---|---|---|
| **주력** | 직접 설계하고 구현할 수 있음 | HTML5, CSS3, JavaScript (ES6+), React, TypeScript |
| **사용 경험** | 프로젝트에 적용해봤고 문서를 보며 쓸 수 있음 | Next.js, Supabase, Tailwind CSS, Git / GitHub |
| **학습 중** | 아직 자신 있게 말하기 어려움 | Zustand, TanStack Query, Vercel, Git 브랜치 전략 |

**왜 나눴는가**

기술 스택에 적는 기준은 **"면접에서 그 항목만으로 3분 이상 설명할 수 있는가"** 입니다.
튜토리얼을 한 번 따라한 것을 주력처럼 적으면, 질문이 들어왔을 때 방어가 되지 않아
오히려 신뢰를 잃습니다.

반대로 **스스로 선을 그어 두면 그 구분 자체가 신뢰가 됩니다.**
"이건 아직 학습 단계입니다"라고 먼저 말한 지원자에게는 그 항목을 깊게 캐묻지 않습니다.

**시각적 구분**

- 각 단계 제목 앞에 색점: 주력(진한 강조색) → 사용 경험(50% 투명) → 학습 중(점선 원)
- 학습 중 그룹의 태그만 `.tags--outline` — 배경 없이 **점선 테두리**로 톤을 낮춤

**퍼센트 바를 쓰지 않은 이유**: "React 80%"에서 80이 무엇을 뜻하는지 설명할 수 없습니다.
면접에서 "왜 80입니까?"라고 물으면 답이 없습니다.

#### Projects

프로젝트 하나가 `<article class="project">` 한 블록입니다.
두 번째 블록에는 `project--reverse`가 붙어 이미지가 반대쪽으로 갑니다(지그재그).

각 블록의 구성:

```
[스크린샷]    2025.03 – 2025.05        ← 기간
              프로젝트 이름
              한 줄 요약
              문제  →  어떤 문제를 풀려고 했는가
              해결  →  어떻게 풀었고 왜 그 기술을 골랐는가
              배운 점 → 무엇을 알게 되었는가
              #React #TypeScript #Supabase
              [사이트 보기] [GitHub]
```

**"문제 → 해결 → 배운 점" 구조가 핵심입니다.**
면접에서 실제로 파고드는 지점이 여기이고, 특히 "배운 점"은 거의 반드시 질문이 나옵니다.
"무엇을 만들었다"보다 **"무엇을 왜 그렇게 결정했다"** 가 평가받습니다.

#### Education

세로선 + 점 마커의 타임라인입니다. 항목 2개(부트캠프/국비교육, 4년제 대학)가
**최신순으로** 배치되어 있습니다.

#### Contact

가운데 정렬. 이메일 주소가 크게 보이고, 옆에 복사 버튼이 있습니다.
호버하면 이메일 아래에 밑줄이 왼쪽에서 오른쪽으로 그어집니다
(`background-size` 애니메이션 — `width`를 애니메이션하는 것보다 부드럽습니다).

---

## 7. 기능 구현 상세

### 7.1 다크 모드 — 깜빡임 없는 테마 적용

**해결하려는 문제**

테마를 JS로만 적용하면, 다크 모드 사용자가 새로고침할 때 **흰 화면이 한 번 번쩍입니다.**
CSS가 먼저 그려지고 JS가 나중에 실행되기 때문입니다. 이를 FOUC(Flash of Unstyled Content)라
부릅니다.

**해결 방법**

`index.html`의 `<head>` 안, **CSS 링크 바로 다음**에 인라인 스크립트를 둡니다.

```js
(function () {
  document.documentElement.classList.add('js');
  try {
    var saved = localStorage.getItem('theme');
    var theme = saved ||
      (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
    document.documentElement.setAttribute('data-theme', theme);
  } catch (e) {}
})();
```

이 스크립트는 **브라우저가 첫 픽셀을 그리기 전에** 동기적으로 실행됩니다.
따라서 `<body>`가 그려지는 시점에는 이미 `data-theme`이 확정되어 있습니다.

> 외부 파일(`theme.js`)로 빼면 안 됩니다. 네트워크 요청이 끼어들어 깜빡임이 돌아옵니다.
> 이 코드가 인라인인 것은 실수가 아니라 필수입니다.

**우선순위**

```
1순위  localStorage에 저장된 사용자 선택
2순위  OS 설정 (prefers-color-scheme)
3순위  라이트
```

`theme.js`는 토글 버튼 동작만 담당하고, **사용자가 한 번도 직접 고른 적이 없을 때만**
OS 설정 변경을 실시간으로 따라갑니다. 이미 선택했다면 그 선택을 존중합니다.

**CSS 쪽 구성** (`tokens.css`)

```css
:root { /* 라이트 값 */ }

@media (prefers-color-scheme: dark) {
  :root:not([data-theme='light']) { /* 다크 값 */ }   /* ← JS가 꺼진 경우 대비 */
}

:root[data-theme='dark'] { /* 다크 값 */ }            /* ← 토글이 항상 이김 */
```

`:not([data-theme='light'])`가 붙은 이유는, OS가 다크인데 사용자가 라이트를 선택한 경우
미디어쿼리가 그 선택을 덮어쓰지 않게 하기 위해서입니다.

**함께 처리하는 것**: 토글 시 `<meta name="theme-color">`도 갱신해
모바일 브라우저 주소창 색까지 맞춥니다.

---

### 7.2 한/영 전환 — 스냅샷 방식

**설계 원칙: HTML에는 한국어를 그대로 둔다**

일반적인 방식은 모든 문구를 JS 객체에 넣고 빈 HTML을 채우는 것입니다. 이 프로젝트는
반대로 갑니다.

```html
<h2 data-i18n="about.title">소개</h2>
```

한국어가 **HTML에 실제로 적혀 있습니다.** 이유는 세 가지입니다.

1. JS가 꺼져 있거나 아직 로드되지 않아도 본문이 읽힙니다
2. 검색엔진이 한국어 본문을 그대로 수집합니다 (SEO)
3. 사전에 한국어를 중복으로 적을 필요가 없습니다

**동작 방식**

```
로드 시  ─→ [data-i18n]을 가진 모든 요소의 한국어 원문을 WeakMap에 스냅샷
EN 클릭 ─→ 사전에서 영어를 꺼내 덮어씀
KO 클릭 ─→ 스냅샷에서 원문을 복원
```

`js/i18n.js`에는 **영어 사전만** 있습니다. 한국어 사전은 존재하지 않습니다.

**번역 누락에 강함**

사전에 키가 없으면 한국어를 그대로 둡니다. 즉 번역을 빠뜨려도
**사이트가 깨지지 않고 그 부분만 한국어로 남습니다.**

```js
if (Object.prototype.hasOwnProperty.call(EN, key)) {
  writeValue(el, EN[key]);
}
// 없으면 아무것도 하지 않음 → 한국어 유지
```

**속성 번역**

텍스트가 아니라 속성(이미지 `alt`, `aria-label` 등)을 번역해야 할 때는
`data-i18n-attr`로 대상 속성을 지정합니다.

```html
<img data-i18n="about.photoAlt" data-i18n-attr="alt" alt="임태형 프로필 사진">
```

**기타 처리**

- `<html lang>` 속성을 함께 갱신합니다 (스크린리더의 발음이 달라집니다)
- 토글 버튼에는 **전환될 언어**를 표시합니다 (한국어 상태 → `EN` 표시)
- 선택은 `localStorage`에 저장됩니다
- 저장값이 없으면 `navigator.language`를 참고하되, 한국어가 기본입니다

---

### 7.3 IntersectionObserver — 하나의 도구, 두 가지 용도

스크롤 관련 기능 두 가지를 **`scroll` 이벤트 없이** 처리합니다.

> `scroll` 이벤트는 스크롤 한 번에 수십~수백 번 발생합니다. 그 안에서 위치를 계산하면
> (`getBoundingClientRect()` 등) 레이아웃 재계산이 반복되어 스크롤이 버벅입니다.
> `IntersectionObserver`는 브라우저가 교차 여부를 알아서 판단해 필요할 때만 알려줍니다.

**용도 1 — 등장 애니메이션**

```js
{ threshold: 0.12, rootMargin: '0px 0px -40px 0px' }
```

요소가 12% 보이면 `.is-visible`을 붙이고 **관찰을 해제**합니다(`unobserve`).
한 번 나타난 것은 다시 사라지지 않으므로 계속 관찰할 이유가 없습니다.

**용도 2 — Scroll spy (현재 섹션 표시)**

```js
{ rootMargin: '-40% 0px -55% 0px', threshold: 0 }
```

화면 위 40%와 아래 55%를 잘라내면 **가운데 5% 띠**만 남습니다.
이 띠에 걸치는 섹션을 "현재 보고 있는 섹션"으로 판단해 해당 메뉴에 밑줄을 긋습니다.

**JS 없이도 깨지지 않는 구조**

`.reveal` 요소를 CSS에서 그냥 `opacity: 0`으로 두면, JS가 실패했을 때
**사이트 전체가 백지**가 됩니다. 이를 막기 위해:

```css
.js .reveal { opacity: 0; transform: translateY(16px); }
.js .reveal.is-visible { opacity: 1; transform: none; }
```

`.js` 클래스는 앞서 본 인라인 스크립트가 붙입니다.
**JS가 실행되지 않으면 `.js`가 없고, 따라서 요소는 숨겨지지도 않습니다.**

`prefers-reduced-motion`이 켜져 있거나 `IntersectionObserver`를 지원하지 않는 브라우저에서는
관찰을 시작하지 않고 전부 즉시 표시합니다.

---

### 7.4 헤더 — 스크롤 시 배경 등장

맨 위에서는 헤더가 투명하고, 8px 이상 스크롤하면 반투명 배경 + 블러 + 아래 경계선이 나타납니다.

여기는 `scroll` 이벤트가 필요하지만, `requestAnimationFrame`으로 **프레임당 한 번**만
실행되도록 묶었습니다.

```js
window.addEventListener('scroll', function () {
  if (!ticking) {
    window.requestAnimationFrame(update);
    ticking = true;   // update()가 끝나면 false로 되돌림
  }
}, { passive: true });
```

`{ passive: true }`는 "이 리스너는 `preventDefault()`를 부르지 않는다"를 브라우저에 알려
스크롤을 기다리지 않고 즉시 처리하게 합니다.

---

### 7.5 모바일 메뉴

768px 미만에서 햄버거 버튼이 나타나고, 누르면 전체 화면 오버레이가 열립니다.

| 처리 | 내용 |
|---|---|
| 상태 표시 | `aria-expanded`가 `true`/`false`로 바뀜. CSS는 이 속성으로 햄버거를 X로 변형 |
| 배경 스크롤 잠금 | `body.is-nav-open { overflow: hidden }` |
| 포커스 이동 | 열면 첫 메뉴 항목으로 포커스 이동 |
| 포커스 가둠 | `Tab`이 화면 밖 요소로 나가지 않도록 순환. 오버레이 위에 계속 보이는 언어·테마 버튼도 순환 대상에 포함 |
| 닫기 | `Esc` 키(포커스를 햄버거로 되돌림), 메뉴 항목 클릭 |
| 폭 변화 대응 | 열린 채로 창을 넓히면 자동으로 정리 (`matchMedia` 감시) |

> 햄버거를 X로 바꾸는 애니메이션은 CSS만으로 처리합니다.
> 막대 2개를 각각 `translateY(3.25px) rotate(45deg)` / `translateY(-3.25px) rotate(-45deg)`
> 시키면 가운데서 교차합니다. 3.25px은 (막대 간격 5px + 막대 두께 1.5px) ÷ 2입니다.

---

### 7.6 이메일 복사

Contact 섹션의 복사 버튼입니다.

```
클릭 → 클립보드에 복사 → 버튼이 "복사됨"으로 바뀌고 강조색으로 변함
     → aria-live 영역에 알림 (스크린리더 사용자도 성공을 인지)
     → 2초 후 원상 복구
```

**보안 컨텍스트 문제와 대비책**

`navigator.clipboard`는 **HTTPS 또는 localhost에서만** 동작합니다.
`index.html`을 파일 탐색기에서 더블클릭해 `file://`로 열면 실패합니다.

그래서 두 단계로 구현했습니다.

```js
if (navigator.clipboard && window.isSecureContext) {
  navigator.clipboard.writeText(email)...     // 정식 경로
} else {
  // 임시 <textarea>를 만들어 document.execCommand('copy')
}
```

두 번째는 구식(deprecated) API지만, 로컬에서 파일을 직접 열어 확인할 때를 위해 남겨뒀습니다.

---

## 8. 접근성

포트폴리오에서 접근성 대응은 **면접에서 말할 거리**가 됩니다.
신입 지원자 중 이 부분을 챙기는 경우가 드물기 때문입니다.

| 항목 | 구현 |
|---|---|
| 시맨틱 마크업 | `header` / `nav` / `main` / `section` / `article` / `footer` |
| 섹션 이름 | 모든 섹션에 `aria-labelledby`로 제목 연결 |
| 본문 바로가기 | 첫 `Tab`에 나타나는 skip link — 메뉴를 건너뛰고 본문으로 |
| 포커스 표시 | `:focus-visible`로 키보드 사용 시에만 표시 (마우스 클릭 시엔 안 나옴) |
| 앵커 위치 보정 | `scroll-margin-top`으로 고정 헤더에 제목이 가려지지 않게 |
| 버튼 상태 | 테마 토글에 `aria-pressed`, 메뉴에 `aria-expanded` / `aria-controls` |
| 동적 알림 | `role="status" aria-live="polite"` 영역으로 복사 성공을 음성 안내 |
| 이미지 대체 텍스트 | 모든 `<img>`에 `alt`, 언어 전환 시 함께 번역 |
| 장식 요소 제외 | 아이콘 SVG와 섹션 번호에 `aria-hidden="true"` |
| 언어 표시 | 전환 시 `<html lang>` 갱신 |
| 모션 민감성 | `prefers-reduced-motion: reduce`에서 모든 애니메이션 무력화 |
| 색 대비 | 라이트/다크 양쪽에서 WCAG AA 기준을 고려해 색상 선정 |

**`prefers-reduced-motion` 처리 방식**

CSS와 JS 양쪽에서 대응합니다.

```css
/* reset.css — 모든 전환·애니메이션을 사실상 0으로 */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
```

```js
// main.js — 관찰을 시작하지 않고 즉시 전부 표시
if (prefersReducedMotion) {
  targets.forEach(el => el.classList.add('is-visible'));
  return;
}
```

---

## 9. 실행과 검증

### 로컬에서 보기

가장 간단한 방법은 `index.html`을 브라우저로 여는 것입니다.
다만 **이메일 복사 버튼은 동작하지 않습니다** (7.6 참고). 정확히 확인하려면 서버로 띄웁니다.

```bash
npx serve .
# → http://localhost:3000
```

Node가 설치되어 있다면 별도 설치 없이 바로 실행됩니다.

### 자동 검증

작성 후 다음 다섯 가지를 스크립트로 확인했고 **전부 통과**했습니다.

| 검사 | 내용 | 결과 |
|---|---|---|
| 1 | HTML의 `data-i18n` 키 60개가 영어 사전에 모두 존재하는가 (양방향) | 누락 0 |
| 2 | `src`/`href`가 가리키는 로컬 파일이 실제로 존재하는가 | 전부 존재 |
| 3 | 앵커·`aria-labelledby`·`aria-controls`가 가리키는 id가 정의되어 있는가 | 전부 유효 |
| 4 | JS의 `getElementById` 대상이 HTML에 존재하는가 | 전부 존재 |
| 5 | 사용하는 CSS 변수가 `tokens.css`에 정의되어 있는가 / 미사용 변수는 없는가 | 불일치 0 |

> 5번에서 미사용 토큰 3개(`--surface`, `--shadow-sm`, 그리고 미적용 `--shadow`)를 발견해
> 정리했습니다. 쓰지 않는 변수가 남아 있으면 읽는 사람이 "이건 어디 쓰이지?"를 확인하느라
> 시간을 씁니다.

### 수동 체크리스트

내용을 채운 뒤 배포 전에 확인하세요.

- [ ] **반응형** — DevTools에서 375 / 768 / 1440px. 가로 스크롤이 생기지 않을 것
- [ ] **다크 모드** — 토글 동작, 새로고침 후 유지, 첫 로드 시 깜빡임 없음
- [ ] **언어 토글** — 모든 문구가 영어로 바뀌고 한국어로 정확히 복귀, 새로고침 후 유지
- [ ] **키보드** — `Tab`만으로 전체 탐색. 첫 `Tab`에 "본문 바로가기"가 나타날 것
- [ ] **모바일 메뉴** — 768px 미만에서 열림, `Esc`로 닫힘, 배경이 스크롤되지 않을 것
- [ ] **Scroll spy** — 스크롤 시 상단 메뉴 밑줄이 따라올 것
- [ ] **모션 감소** — DevTools > Rendering > Emulate `prefers-reduced-motion`에서 애니메이션 정지
- [ ] **Lighthouse** — Performance / Accessibility / Best Practices / SEO 각 90점 이상
- [ ] **실기기** — 배포 후 실제 휴대폰에서 열어볼 것

---

## 10. 자주 하는 수정 방법

### 사이트 전체 색감 바꾸기

`css/tokens.css`에서 `--accent` 계열 3개를 수정합니다. 라이트와 다크 각각 있습니다.

```css
:root {
  --accent: #2563eb;        /* 기본 강조색 */
  --accent-hover: #1d4ed8;  /* 호버 시 (조금 더 진하게) */
  --accent-soft: #eff6ff;   /* 배경용 (아주 옅게) */
}
```

다크 모드 값은 **라이트보다 밝게** 잡아야 합니다. 어두운 배경에서 진한 색은 읽히지 않습니다.

### 프로젝트 추가하기

1. `index.html`의 `<article class="project">` 블록을 통째로 복사해 아래에 붙여넣습니다
2. **짝수 번째 블록에만** `project--reverse`를 붙입니다 (지그재그 유지)
3. `data-i18n` 키를 새 이름으로 바꿉니다 (예: `projects.p3Title`)
4. `js/i18n.js`의 `EN` 객체에 같은 키의 영어 문구를 추가합니다

### 스킬 태그 옮기기

학습이 끝난 항목은 위 단계로 올립니다. `<li class="tag">` 한 줄을 다른 `<ul>`로 옮기면 됩니다.

```html
<!-- 학습 중 → 사용 경험 -->
<li class="tag">Vercel</li>
```

> **배포를 마치면 Vercel을 반드시 "사용 경험"으로 옮기세요.**
> 직접 배포해본 것은 실제 경험입니다.

`.tags--outline`(점선)은 학습 중 그룹의 `<ul>`에 붙어 있으므로, 항목을 옮기면
스타일도 자동으로 따라 바뀝니다.

### 번역 문구 추가하기

```html
<p data-i18n="새.키이름">한국어 문구</p>
```

```js
// js/i18n.js
var EN = {
  '새.키이름': 'English text',
};
```

키 이름은 `섹션.항목` 형태로 짓습니다. 사전에 추가하지 않으면 그 문구만 한국어로 남습니다.

### 섹션 추가하기

1. `index.html`에 `<section class="section" id="새id" aria-labelledby="새id-title">` 추가
2. 배경을 다르게 하려면 `section--alt` 클래스를 함께 붙입니다
3. 헤더 `<ul class="nav__list">`에 `<a class="nav__link" href="#새id">` 추가
   → **scroll spy가 자동으로 인식합니다** (`main.js`가 메뉴 링크의 `href`를 읽어 섹션을 찾습니다)
4. 섹션 번호(`section__num`)를 순서에 맞게 조정합니다

### 이력서 PDF 넣기

1. `assets/resume.pdf`로 파일을 저장합니다
2. `index.html:195` 부근의 주석을 해제합니다

---

## 11. 남은 작업

### 우선순위 순

| # | 작업 | 위치 |
|---:|---|---|
| 1 | **프로젝트 2건 내용** — 이름 / 기간 / 요약 / 문제·해결·배운 점 / 링크 | `index.html:284-380` |
| 2 | **프로젝트 스크린샷** | `assets/img/project-1.png`, `project-2.png` |
| 3 | **소개 문단 3개** (초안이 들어 있으니 고쳐 쓰면 됨) | `index.html:179-193` |
| 4 | **Hero 한 줄 소개** | `index.html:139` |
| 5 | **프로필 사진** (정사각, 800px 이상) | `assets/img/profile.jpg` |
| 6 | **학력 · 부트캠프 이력** | `index.html:405-422` |
| 7 | **영어 번역 갱신** | `js/i18n.js`의 `EN` 객체 |
| 8 | (선택) 이력서 PDF | `assets/resume.pdf` + `index.html:195` 주석 해제 |
| 9 | 배포 후 Open Graph URL·이미지 갱신 | `index.html:23` |
| 10 | 배포 후 README에 라이브 링크 추가 | `README.md:6` |

### 가장 중요한 것

**1번(프로젝트 내용)에 가장 많은 시간을 쓰세요.**
디자인과 기능은 이미 완성되어 있습니다. 채용 담당자가 실제로 읽고 판단하는 것은
프로젝트 섹션의 텍스트이고, 면접 질문도 거기서 나옵니다.

특히 **"배운 점"** 은 형식적으로 채우지 마세요.
"React를 배웠다"가 아니라 "이런 문제를 겪고 이렇게 해결하면서 무엇을 알게 되었다"로 쓰면
그 자체가 면접 답변이 됩니다.

### 자리표시자 찾는 법

```bash
grep -rn "TODO" index.html js/
```

`[TODO]`로 표시된 곳이 채워야 할 자리입니다.

---

## 12. 배포 절차

목표 중 하나가 **직접 배포를 경험해보는 것**이므로, 순서를 자세히 적습니다.

### 1) GitHub에 올리기

```bash
cd my-profile-site

git add .
git commit -m "feat: 포트폴리오 사이트 초기 구현"

# GitHub에서 저장소를 먼저 만든 뒤 (예: THLIMM/portfolio)
git remote add origin https://github.com/THLIMM/portfolio.git
git branch -M main
git push -u origin main
```

> `git init`은 이미 되어 있습니다. 아직 커밋은 하지 않은 상태입니다.

### 2) Vercel에 연결하기

1. [vercel.com](https://vercel.com)에 GitHub 계정으로 로그인
2. **Add New → Project** → 방금 만든 저장소 선택 (Import)
3. 설정 화면에서 **아무것도 건드리지 않고** Deploy
   - Framework Preset: `Other`
   - Build Command: 비워둠
   - Output Directory: 비워둠
   - → 빌드 도구가 없으므로 파일을 그대로 서빙합니다
4. 1분 내로 `https://프로젝트명.vercel.app` 주소가 나옵니다

### 3) 이후

`main` 브랜치에 push할 때마다 **자동으로 재배포**됩니다.
Pull Request를 만들면 미리보기 URL도 따로 생성됩니다.

### 4) 배포 후 할 일

- [ ] `index.html:23` — Open Graph의 `og:url`, `og:image` 주석 해제 및 실제 주소 입력
- [ ] `README.md:6` — 라이브 링크 추가
- [ ] **Skills의 Vercel 태그를 "학습 중" → "사용 경험"으로 이동**
- [ ] 실제 휴대폰에서 열어 확인

---

## 13. 면접에서 설명할 수 있는 포인트

이 사이트를 만들면서 나온, **질문받으면 답할 수 있어야 하는** 항목들입니다.
반대로 말하면, 이 사이트는 아래 주제들의 근거가 되어 줍니다.

| 주제 | 무엇을 말할 수 있는가 |
|---|---|
| **기술 선택** | 왜 React를 쓰지 않았는가 → 정적 원페이지에 프레임워크는 오버엔지니어링이라 판단 |
| **FOUC 방지** | 왜 인라인 스크립트가 `<head>`에 있어야 하는가 → 첫 페인트 전 동기 실행 |
| **점진적 향상** | `.js` 클래스로 JS 실패 시에도 콘텐츠가 보이도록 한 구조 |
| **SEO / i18n** | 왜 한국어를 HTML에 남기고 영어만 사전으로 뒀는가 |
| **성능** | `scroll` 이벤트 대신 `IntersectionObserver`를 쓴 이유, `requestAnimationFrame` 스로틀링, `{ passive: true }` |
| **CSS 설계** | 커스텀 프로퍼티로 테마를 구성하는 방법, `clamp()`로 미디어쿼리 없이 타입 스케일 만들기 |
| **접근성** | `aria-expanded` / `aria-pressed` / `aria-live`의 용도, 포커스 관리, `prefers-reduced-motion` |
| **모바일 대응** | `100vh` 대신 `100svh`를 쓴 이유 (주소창 높이 변화) |
| **보안 컨텍스트** | Clipboard API가 HTTPS에서만 동작하는 이유와 대비책 |
| **배포** | Git 연동 자동 배포가 어떻게 동작하는가 |

> 이 표의 항목 중 **설명하기 어려운 것이 있다면, 그 부분의 코드를 다시 읽어보세요.**
> 자기가 만든 사이트에 대해 답하지 못하는 것이 가장 나쁜 경우입니다.

---

## 14. 알려진 제약

정직하게 남겨 둡니다. 대부분 의도된 트레이드오프입니다.

| 제약 | 설명 | 대응 |
|---|---|---|
| 웹폰트가 CDN 의존 | Pretendard를 jsDelivr에서 불러옵니다. CDN 장애 시 시스템 폰트로 폴백됩니다 | 폴백 스택을 지정해 둠. 필요하면 폰트를 로컬에 두는 방법도 있음 |
| 번역이 수동 관리 | 한국어를 고치면 `i18n.js`의 영어도 직접 고쳐야 합니다 | 키가 없으면 한국어가 남으므로 깨지지는 않음 |
| `file://`에서 복사 실패 | 파일을 직접 열면 Clipboard API가 막힙니다 | `execCommand` 대비책 있음. 서버로 띄우면 정상 동작 |
| 프로젝트 추가가 수동 | 데이터 배열이 아니라 HTML 블록 복사 방식입니다 | 프로젝트가 5개를 넘어가면 JS 데이터 기반으로 바꾸는 것을 권장 |
| 검색엔진에 노출되는 것은 한국어만 | 영어는 JS로 치환되므로 크롤러가 보지 못합니다 | 국내 지원이 주목적이라 문제 없음. 영어 SEO가 필요해지면 별도 페이지 분리 필요 |
| 이미지가 자리표시자 SVG | 실제 사진·스크린샷 미교체 상태 | [11. 남은 작업](#11-남은-작업) 참고 |

---

## 부록: 용어 정리

| 용어 | 뜻 |
|---|---|
| **FOUC** | Flash of Unstyled Content. 스타일이 적용되기 전 화면이 잠깐 번쩍이는 현상 |
| **BEM** | Block-Element-Modifier. `.block__element--modifier` 형태의 CSS 클래스 이름 규칙 |
| **디자인 토큰** | 색·간격·글꼴 크기 같은 값을 이름 붙여 재사용하는 단위. 여기서는 CSS 변수 |
| **점진적 향상** | 기본 기능은 항상 동작하게 하고, 가능한 환경에서만 추가 기능을 얹는 방식 |
| **보안 컨텍스트** | HTTPS 또는 localhost 환경. 일부 브라우저 API가 여기서만 동작 |
| **scroll spy** | 스크롤 위치에 따라 현재 보고 있는 섹션을 내비게이션에 표시하는 기능 |
| **`svh`** | Small Viewport Height. 모바일 주소창이 펼쳐진 상태 기준의 화면 높이 단위 |
| **WCAG AA** | 웹 접근성 지침의 중간 등급. 본문 텍스트 대비 4.5:1 이상을 요구 |
